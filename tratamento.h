#ifndef TRATAMENTO_H_
#define TRATAMENTO_H_

/*Função que trata a string e retorna a string desejada*/
char *retornaString(char *string);

/*Função que organiza o arquivo e suas funções da maneira desejada, iniciando todo os processos essenciais do projeto,
e os arranjando de maneira prática.*/
void tratamentoString(char*, char*, char*, char*);

#endif